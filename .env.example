# Voidlet environment variables.
# Copy to .env.local for local development. Values are filled in by phase:
#   Phase 2 — Supabase (auth + database)
#   Phase 3 — Stripe (payments)
# No variables are required for Phase 1.
